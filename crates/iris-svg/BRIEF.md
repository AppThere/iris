## `iris-svg` — SVG adapter

**Gate:** `iris-vector` milestone complete (Phase 3).

### Phase 1 milestone (pre-gate stub only)

No implementation in Phase 1. Crate exists as stub.

### Phase 3 milestone

Read SVG 1.1 into a vector `LayerTree`. Write vector layers to SVG 1.1.
Raster layers written as base64-encoded PNG `<image>` elements.
