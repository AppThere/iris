[package]
name        = "iris-canvas"
version     = "0.1.0"
description = "Iris infinite canvas: viewport, scroll model, compositor"
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true
authors.workspace     = true

[dependencies]
thiserror = { workspace = true }
tracing   = { workspace = true }
uuid      = { workspace = true }
iris-pixel  = { workspace = true }
iris-vector = { workspace = true }
# appthere-canvas = { workspace = true }  # Uncomment once gate is open
# vello  = { workspace = true }           # Uncomment once gate is open
# wgpu   = { workspace = true }           # Uncomment once gate is open
