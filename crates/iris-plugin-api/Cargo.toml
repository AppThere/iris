[package]
name        = "iris-plugin-api"
version     = "0.1.0"
description = "WASI plugin sandbox API surface"
edition.workspace     = true
rust-version.workspace = true
license.workspace     = true
authors.workspace     = true

[dependencies]
thiserror = { workspace = true }
tracing   = { workspace = true }
uuid      = { workspace = true }
iris-pixel  = { workspace = true }
iris-vector = { workspace = true }

# Platform-tiered wasmtime — Phase 5 only (ADR 005)
# Desktop + Android: full Cranelift JIT
# [target.'cfg(not(target_os = "ios"))'.dependencies]
# wasmtime = { version = "25", features = ["cranelift", "component-model"] }

# iOS: Pulley interpreter, no JIT (Option A — blocked on track record)
# [target.'cfg(target_os = "ios")'.dependencies]
# wasmtime = { version = "25", default-features = false, features = ["pulley", "component-model"] }
